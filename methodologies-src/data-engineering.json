{
  "category": "data-engineering",
  "label": "Data-engineering methodologies",
  "methodologies": [
    {
      "id": "dataset-curation-pipeline",
      "name": "Dataset Curation Pipeline",
      "aliases": [
        "three-pillar dataset curation",
        "QCQ dataset pipeline"
      ],
      "category": "data-engineering",
      "summary": "Build any ML or LLM training dataset around three goals: quality, coverage, and quantity. Run the data through a clear pipeline that inspects, deduplicates, cleans, filters, then formats it. The pipeline outputs a versioned dataset, keeps the original raw data unchanged, and records every step so you can audit it. The guiding line is that reading the data by hand gives more value for less glory than almost anything else in machine learning. This pipeline forces that reading to happen.",
      "intent": "Turn raw data into a versioned training dataset. Run it through an inspect, deduplicate, clean, filter, and format pipeline that openly trades off quality, coverage, and quantity.",
      "when_to_apply": "Use this when preparing any real training, fine-tuning, or evaluation dataset. That covers a pre-training corpus, a fine-tuning instruction set, a preference set, or a RAG evaluation set. Run it before training, not after. Don't apply it for one-off prototypes whose dataset you will throw away, because the audit trail is wasted there. One exception: if the dataset is small enough for one engineer to read every example, you can skip the formal pipeline, but still write down that the inspection happened.",
      "inputs": [
        {
          "name": "Raw data sources",
          "description": "The source documents, transcripts, prompts, and examples. This is the raw material before any curation."
        },
        {
          "name": "Target task definition",
          "description": "What the dataset will train or evaluate. This is what sets the meaning of 'quality' and 'coverage'."
        },
        {
          "name": "Coverage map",
          "description": "A list of the dimensions the dataset must span, such as formats, edge cases, demographics, languages, and error types."
        }
      ],
      "outputs": [
        {
          "name": "Versioned dataset artefact",
          "description": "The curated dataset, checked into a registry with a version tag, a schema, statistics, and a trail back to the raw inputs."
        },
        {
          "name": "Curation log",
          "description": "An audit trail of every step. It records what was removed, what was kept, what was generated, and the rule behind each choice."
        },
        {
          "name": "Preserved raw",
          "description": "The raw inputs, kept unchanged. This lets you re-run the pipeline and revisit the curation rules later."
        }
      ],
      "steps": [
        {
          "name": "Inspect raw data manually",
          "description": "Before any automation, an engineer reads a real sample of the raw data. Most surprises about the corpus show up here, such as encoding issues, duplicates, bias, and irrelevant material. This is the step with the most value for the least glory."
        },
        {
          "name": "Deduplicate",
          "description": "Remove exact duplicates first, then near-duplicates by word overlap or meaning. Duplicates inflate the apparent size and push the model toward whatever is repeated. Log the dedupe rates. High rates usually mean the sources were chosen poorly."
        },
        {
          "name": "Clean",
          "description": "Strip control characters, fix encoding, repair broken structure, and redact personal data. Cleaning is mechanical and rule-based. It does not yet touch quality or coverage."
        },
        {
          "name": "Filter on quality",
          "description": "Apply quality filters. Some are simple rules, such as length, vocabulary, or the share of meaningful words. Others use a model, such as a classifier or an LLM that scores each example. Keep only examples above the cutoff. Write down the cutoff, and check a hand-labelled sample to see how often the filter is wrong in each direction.",
          "uses_patterns": [
            "llm-as-judge",
            "frozen-rubric-reflection"
          ]
        },
        {
          "name": "Cover the dimensions",
          "description": "Check the filtered set against the coverage map. For any thin dimension, either add data by generating or upsampling it, or note plainly that the model will be weak there. Coverage gaps you do not flag turn into silent failures in production.",
          "uses_patterns": [
            "dimensional-synthetic-eval-set"
          ]
        },
        {
          "name": "Format and version",
          "description": "Convert to the training format you need, such as JSONL of messages, parquet, or tokenised shards. Compute the dataset statistics. Tag the version. Register it in the dataset store with a full trail. Keep the raw and intermediate stages so you can re-run the pipeline."
        }
      ],
      "principles": [
        "Read the data by hand. Every dataset starts with eyes on raw examples.",
        "Three goals, not two. Quality and quantity are easy to remember; coverage is the one people forget.",
        "Preserve the raw data. You can only reverse curation if the original survives.",
        "Keep the curation log. A dataset whose curation rules are lost cannot be fixed."
      ],
      "applies_to": [
        "llm-app",
        "agent",
        "rag-system",
        "eval-harness"
      ],
      "related_patterns": [
        "dimensional-synthetic-eval-set",
        "streaming-feature-pipeline",
        "llm-as-judge",
        "frozen-rubric-reflection"
      ],
      "related_compositions": [
        "production-llm-platform",
        "eval-and-observability"
      ],
      "related_methodologies": [
        "instruct-dataset-generation-pipeline",
        "evaluation-driven-development"
      ],
      "known_failure_modes": [
        {
          "pattern": "automating-broken-process",
          "note": "Inheriting a curation pipeline from a previous project and running it blind — the new corpus needs different filters, the old ones silently throw away the wrong examples."
        },
        {
          "pattern": "errors-swept-under-the-rug",
          "note": "Filtering on quality without auditing the rejected examples — the filter throws away minority-class cases and the model learns the bias."
        },
        {
          "pattern": "reward-hacking",
          "note": "Setting an LLM-judge quality filter that the data generator can game — high scores, low real quality."
        }
      ],
      "maturity": "proven",
      "sources": [
        {
          "type": "book",
          "title": "AI Engineering",
          "url": "https://www.oreilly.com/library/view/ai-engineering/9781098166298/",
          "chapter": "Ch 8 'Dataset Engineering'",
          "quote": "Manual inspection of data has probably the highest value to prestige ratio of any activity in machine learning"
        },
        {
          "type": "blog",
          "title": "Dataset Engineering: The Art and Science of Data Preparation — chapter 8 notes",
          "url": "https://alexstrick.com/posts/2025-02-05-notes-on-ai-engineering-chip-huyen-chapter-8-dataset-engineering.html"
        }
      ],
      "verification_status": "verified",
      "date_added": "2026-05-24",
      "last_updated": "2026-05-27",
      "tags": [
        "dataset",
        "curation",
        "quality",
        "coverage"
      ],
      "diagram": {
        "type": "flow",
        "mermaid": "flowchart TD\n  raw[Raw data sources] --> preserve[Preserve raw unchanged]\n  raw --> inspect[Manual inspection of sample]\n  inspect --> dedupe[Deduplicate exact + near]\n  dedupe --> clean[Clean: encoding, PII, structure]\n  clean --> qfilt[Quality filter:\\nheuristic + LLM-judge]\n  qfilt -->|rejected| audit[Audit rejected sample]\n  audit -->|filter wrong| qfilt\n  qfilt --> cov[Check coverage map]\n  cov -->|gap| aug[Augment / upsample]\n  aug --> dedupe\n  cov -->|covered| fmt[Format + tokenize]\n  fmt --> ver[Version + register]\n  ver --> art[Versioned dataset artefact]\n  qfilt --> log[Curation log]\n  dedupe --> log"
      },
      "example_scenario": "A team is preparing the training corpus for a domain-specialised assistant for healthcare procedural documentation. The raw sources are a mix of internal wiki exports, scraped procedure manuals, and chat transcripts — roughly 240,000 raw documents of widely varying quality. They follow the three-pillar curation pipeline. First an engineer reads a random sample of 200 documents and writes a short note: there are encoding artefacts from the wiki export, near-duplicates from versioned procedure copies, PII in the transcripts, and entire format families (regulatory citations) that are over-represented while pediatric procedures are under-represented.\n\nThe pipeline then runs in order: exact-match dedupe drops 18% of documents, near-dupe dedupe drops another 11%; cleaning fixes encoding and redacts PII; an LLM-judge quality filter scores each document against a rubric and keeps the top 62%, with a hand-audited sample of 100 rejected examples confirming the filter is not throwing away pediatric content disproportionately. The coverage check against the predefined map flags pediatric procedures and one rare therapeutic category as under-represented; the team augments those by upsampling and prompted paraphrase, then runs the augmented samples back through dedupe and the quality filter so they pass the same gates. The final dataset is formatted as JSONL of messages, tagged v0.3.1, and registered with a curation log that names every transformation, every threshold, and every reviewer — so when a future audit asks why a particular family of cases is sparse, the answer is reachable rather than lost.",
      "deep_dive": "**The three pillars.** Curation trades off *quality* (each example is correct and well-formed), *coverage* (the set spans the required dimensions — formats, edge cases, demographics, languages, error types), and *quantity* (enough examples to train on). Coverage is the pillar most often dropped.\n\n**The pipeline stages, in order:**\n- **Manual inspection.** An engineer reads a meaningful sample of the raw corpus before any automation. This is the highest value-to-prestige step: encoding artefacts, duplicates, bias, and irrelevant material surface here.\n- **Deduplication.** Exact-match dedupe first, then near-duplicate dedupe via n-gram overlap or semantic (embedding) similarity. Duplicates inflate apparent quantity and bias the model toward repeated content; high dedupe ratios usually indicate bad source selection.\n- **Cleaning.** Mechanical, rule-based repair: strip control characters, fix encoding, repair structural damage, redact PII. Quality and coverage are *not* addressed here.\n- **Quality filtering.** Combine heuristic filters (length, vocabulary, ratio of meaningful tokens) with model-based scoring (a classifier or an LLM-as-judge against a frozen rubric). Keep examples above a documented threshold, and hand-label a sample to measure false-positive/false-negative rates — the filter must not disproportionately drop minority-class cases.\n- **Coverage check and augmentation.** Slice the filtered set by the coverage map; under-represented dimensions get augmentation (synthesis or upsampling) — and augmented samples are re-run through dedupe and quality filtering so they pass the same gates — or an explicit acknowledgement of weakness.\n- **Format and version.** Convert to the target format (JSONL of messages, parquet, tokenised shards), compute statistics, tag the version, and register in the dataset store with full lineage. Raw and intermediate stages are preserved for reproducibility.\n\n**Auditability invariants.** The raw inputs are never mutated, and every transformation is written to a curation log (what was removed/kept/synthesised, on what rule, by which reviewer), so curation decisions can be re-litigated long after the dataset ships."
    },
    {
      "id": "instruct-dataset-generation-pipeline",
      "name": "Instruct Dataset Generation Pipeline",
      "aliases": [
        "seven-stage instruction dataset",
        "noisy-docs to instruct"
      ],
      "category": "data-engineering",
      "summary": "A seven-stage pipeline that turns a messy pile of documents into a clean dataset for teaching a model to follow instructions, ready for supervised fine-tuning. The stages are: pull out candidate examples, remove duplicates, remove anything that overlaps your test set, filter for quality, check coverage, add data where it is thin, then package it. The pipeline rests on a clear finding. For a task-specific fine-tune, 100 to 100,000 carefully prepared examples beat a much larger but dirty pile. Quality, not quantity, is the lever.",
      "intent": "Turn a raw document corpus into a clean, leak-free, well-covered instruction-tuning dataset through seven clear stages.",
      "when_to_apply": "Use this when preparing the instruction-tuning dataset for a supervised fine-tune of an LLM, such as a domain model, an LLM twin, or a task-specific assistant. It helps most when the starting material is messy, such as scraped pages, exported chats, or mixed-quality archives. Do not use it when you already have a hand-curated instruction set of proven quality. Two exceptions: skip the add-data stage when the corpus is large and evenly spread, and skip the overlap-removal stage only when the test set was made after the fact and shares nothing public, which is rare.",
      "inputs": [
        {
          "name": "Source document corpus",
          "description": "Raw documents the team owns or has licensed, such as articles, posts, transcripts, manuals, and internal wikis."
        },
        {
          "name": "Evaluation set",
          "description": "A held-out set used to score the fine-tuned model. You need it to remove training examples that overlap it, so the training set does not leak in."
        },
        {
          "name": "Coverage map",
          "description": "The dimensions the instruction set must span, such as task types, domain subareas, instruction styles, and difficulty bands."
        },
        {
          "name": "Quality filter (model or rubric)",
          "description": "An LLM judge or a classifier that scores each candidate instruction against the rubric."
        }
      ],
      "outputs": [
        {
          "name": "Instruct dataset artefact",
          "description": "A versioned instruction-and-response set, typically 100 to 100k samples, ready for supervised fine-tuning."
        },
        {
          "name": "Curation log",
          "description": "A full audit of the extraction rules, the dedupe rates, the overlap hits, the filter cutoffs, and the sources of any added data."
        },
        {
          "name": "Coverage report",
          "description": "Counts per dimension that confirm the dataset spans the coverage map, or flag where it falls short."
        }
      ],
      "steps": [
        {
          "name": "Extract candidate instruction-response pairs",
          "description": "Go through the source corpus and create candidate pairs. For each document, prompt an LLM with templates to write instructions whose answers come from that document. The corpus is the source of truth. The LLM rewrites; it does not invent."
        },
        {
          "name": "Deduplicate",
          "description": "Remove exact duplicates first, then near-duplicates by word overlap or meaning. Duplicate instructions teach the model to lean on one style and lose its range."
        },
        {
          "name": "Decontaminate against the evaluation set",
          "description": "Compare candidates to the held-out test set and drop anything that overlaps. Skip this step and your scores measure leakage, not real ability. Match both sides on word sequences or meaning, and log every hit."
        },
        {
          "name": "Quality-filter",
          "description": "Score every remaining candidate with an LLM judge against a rubric, covering how clear the instruction is, how correct the response is, and how well it follows the format. Keep only candidates above the cutoff. By hand, check a sample of accepted and rejected items to confirm the filter is not dropping the wrong cases.",
          "uses_patterns": [
            "llm-as-judge",
            "frozen-rubric-reflection"
          ]
        },
        {
          "name": "Explore for coverage",
          "description": "Slice the filtered set by the coverage map and find the thin dimensions. These gaps feed the next stage. They are not a final pass-or-fail check.",
          "uses_patterns": [
            "dimensional-synthetic-eval-set"
          ]
        },
        {
          "name": "Augment under-represented dimensions",
          "description": "For each gap, make more samples. Generate them with a prompted LLM, paraphrase existing examples, or pull more from the corpus with targeted templates. Run the new samples back through dedupe, overlap removal, and the quality filter. They pass the same gates as everything else."
        },
        {
          "name": "Package and version",
          "description": "Format the data the way the fine-tuning trainer expects, usually JSONL of message lists. Compute the statistics, such as sample count, token distribution, and counts per dimension. Tag the version. Register it in the dataset store with a trail back to the source corpus and the rejected-candidate logs.",
          "uses_patterns": [
            "streaming-feature-pipeline"
          ]
        }
      ],
      "principles": [
        "100 to 100k carefully prepared samples beat a large dirty corpus. Quality, not quantity, is the lever.",
        "Remove test-set overlap before quality filtering. A leaked test example can score high on quality and still ruin the evaluation.",
        "Every added sample passes the same gates as the originals. There is no special path for generated data.",
        "Coverage is a stage, not an afterthought. The gaps you do not flag become the model's blind spots."
      ],
      "applies_to": [
        "llm-app",
        "agent",
        "rag-system"
      ],
      "related_patterns": [
        "dimensional-synthetic-eval-set",
        "streaming-feature-pipeline",
        "llm-as-judge",
        "frozen-rubric-reflection"
      ],
      "related_compositions": [
        "production-llm-platform"
      ],
      "related_methodologies": [
        "dataset-curation-pipeline",
        "llm-twin-end-to-end-construction",
        "finetune-last-resort-escalation"
      ],
      "known_failure_modes": [
        {
          "pattern": "automating-broken-process",
          "note": "Generating instructions with the model that will later be fine-tuned — the model learns to produce what it already produced, no real capability is added."
        },
        {
          "pattern": "reward-hacking",
          "note": "The LLM judge filtering quality is gameable by the LLM generator authoring candidates; both ends collude on a score that doesn't reflect real quality."
        },
        {
          "pattern": "errors-swept-under-the-rug",
          "note": "Skipping the rejected-sample audit — the filter throws away exactly the cases the production model will hit, and nobody notices until launch."
        }
      ],
      "maturity": "emerging",
      "sources": [
        {
          "type": "book",
          "title": "LLM Engineer's Handbook",
          "url": "https://www.packtpub.com/en-us/product/llm-engineers-handbook-9781836200079",
          "chapter": "Ch 3 'Data Engineering'; Ch 5 'Supervised Fine-tuning'",
          "quote": "Task-specific models can be fine-tuned with a much smaller dataset, typically ranging from 100 to 100,000 samples",
          "accessed": "2026-05-24"
        },
        {
          "type": "blog",
          "title": "Iusztin: Generate High-Quality Instruct Datasets for Fine-Tuning LLMs",
          "url": "https://www.decodingai.com/p/from-noisy-docs-to-fine-tuning-datasets",
          "quote": "transform noisy documents collected from Notion and the Internet (through crawling) into a high-quality instruction dataset ... the quality of your dataset is the most critical aspect ... run multiple summaries for each document to augment our dataset ... apply a second layer of filtering",
          "accessed": "2026-05-24"
        },
        {
          "type": "repo",
          "title": "PacktPublishing/LLM-Engineers-Handbook (book companion repo)",
          "url": "https://github.com/PacktPublishing/LLM-Engineers-Handbook",
          "quote": "Instruct Dataset Pipeline: Generates instruction-following datasets via poetry poe run-generate-instruct-datasets-pipeline for supervised fine-tuning phases",
          "accessed": "2026-05-24"
        }
      ],
      "verification_status": "verified",
      "date_added": "2026-05-24",
      "last_updated": "2026-05-27",
      "tags": [
        "instruct-dataset",
        "sft",
        "fine-tuning",
        "augmentation"
      ],
      "diagram": {
        "type": "flow",
        "mermaid": "flowchart TD\n  src[Source document corpus] --> s1[1. Extract candidate\\n(instruction, response) pairs]\n  s1 --> s2[2. Deduplicate exact + near]\n  s2 --> s3[3. Decontaminate against eval set]\n  evalset[Held-out evaluation set] --> s3\n  s3 --> s4[4. Quality-filter via LLM judge]\n  rubric[Quality rubric] --> s4\n  s4 --> s5[5. Explore for coverage]\n  covmap[Coverage map] --> s5\n  s5 -->|gaps found| s6[6. Augment under-represented]\n  s6 --> s2\n  s5 -->|covered| s7[7. Package + version]\n  s7 --> art[Instruct dataset artefact\\n100–100k samples]\n  s4 --> audit[Audit accepted + rejected sample]\n  audit --> s4"
      },
      "example_scenario": "A solo builder is creating an LLM-twin assistant trained to imitate the builder's own technical writing style. The raw material is a noisy corpus of 3,400 documents — blog posts, newsletter issues, internal notes, scraped web pages quoting the builder, exported chat conversations. They follow the seven-stage instruct-dataset pipeline. Stage one prompts an LLM with a template per document to author candidate (instruction, response) pairs whose responses come from the document content; this yields about 18,000 candidates. Stage two dedupes exact and near-duplicates, dropping 4,200. Stage three decontaminates against a held-out evaluation set of 200 reference (instruction, response) pairs hand-written for the project — any candidate sharing n-gram overlap with an eval item is dropped, with every hit logged.\n\nStage four runs an LLM judge against a rubric (clarity of instruction, faithfulness of response to source, style adherence) and keeps the top 6,800 candidates above the threshold; the builder hand-audits 50 accepted and 50 rejected items and confirms the filter is not throwing away the casual-tone examples that are most stylistically valuable. Stage five slices the surviving set by the coverage map (topics, instruction styles, difficulty) and flags two underrepresented topic clusters. Stage six augments those clusters by prompted paraphrase and runs the new samples back through dedupe, decontamination, and the quality filter — they get no special path. Stage seven formats as JSONL of message lists, computes statistics (final count ~7,400 samples), tags the version, and registers the dataset alongside its full curation log. The builder honours the rule: quality, not quantity. Earlier experiments with the raw 18,000 candidates produced an SFT model that mimicked surface tics without capturing voice; the 7,400-sample curated set produced a recognisably stylistic twin.",
      "deep_dive": "**The core lever.** For task-specific supervised fine-tuning (SFT), a curated set of roughly 100 to 100,000 samples outperforms a dramatically larger but dirty corpus. The pipeline exists to spend effort on quality, decontamination, and coverage rather than raw volume.\n\n**The seven stages, in order:**\n1. **Extract candidate (instruction, response) pairs.** Walk the corpus; per document, prompt an LLM with templates to author instructions whose answers are grounded in that document. The corpus is the source of truth — the LLM rewrites, it does not invent. (A known failure mode: generating instructions with the *same* model you will later fine-tune teaches it to reproduce what it already produces.)\n2. **Deduplicate.** Exact-match first, then near-duplicate by n-gram or embedding similarity. Duplicate instructions over-emit one style at the cost of generality.\n3. **Decontaminate against the eval set.** Hash both candidates and held-out eval items on n-grams or embeddings; drop any overlap and log every hit. This runs *before* quality filtering, because a leaked eval example can score high on the quality rubric and still corrupt the evaluation.\n4. **Quality-filter.** Score each candidate with an LLM-as-judge against a frozen rubric (instruction clarity, response correctness/faithfulness to source, format adherence). Keep above-threshold candidates and hand-audit a sample of accepted and rejected items — the judge is gameable by the generator, so collusion on an inflated score is a real risk.\n5. **Explore for coverage.** Slice the surviving set by the coverage map (task types, domain subareas, instruction styles, difficulty bands) to locate under-represented regions. Gaps are inputs to stage 6, not a final acceptance criterion.\n6. **Augment under-represented dimensions.** Fill each gap by prompted synthesis, paraphrase of existing examples, or targeted-template upsampling — then route the augmented samples back through dedupe, decontamination, and quality filtering. No synthetic data gets a special path.\n7. **Package and version.** Format as the SFT trainer expects (typically JSONL of message lists), compute statistics (sample count, token distribution, per-dimension counts), tag the version, and register with lineage back to the source corpus and the rejected-candidate logs.\n\n**Contrast with general dataset curation.** This pipeline is specialised for instruction data feeding an SFT run: it adds an explicit generation step (stage 1) and an explicit decontamination-against-eval step (stage 3) that a general training-set curation pipeline does not require."
    }
  ]
}
