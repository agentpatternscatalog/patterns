{
  "category": "fine-tuning",
  "label": "Fine-tuning methodologies",
  "methodologies": [
    {
      "id": "pretrain-then-adapt-methodology",
      "name": "Pretrain Then Adapt",
      "aliases": ["pretrain-and-fine-tune", "base-then-specialize"],
      "category": "fine-tuning",
      "summary": "Build a general-purpose base model first via next-token prediction on a broad unlabeled corpus, then specialize it via supervised fine-tuning on task-specific data. One pretrained base feeds many fine-tuned variants — a classifier, an instruction follower, a domain assistant — without paying the pretraining cost again. The methodology separates the expensive, knowledge-acquiring phase from the cheap, behaviour-shaping phase so that the specialization step can be iterated quickly and per use case.",
      "intent": "Amortize the cost of acquiring general language knowledge across many downstream tasks by training one base and adapting it cheaply per task.",
      "when_to_apply": "When you control the model weights and have a domain or task family where a generic instruction-tuned model is underperforming, and you have access to either an existing base checkpoint or sufficient unlabeled domain text. Don't apply when a hosted instruction-tuned API model meets your quality bar — the adaptation cost is not justified. Exceptions: regulated environments forcing on-prem weights, or research investigating base-model behaviour itself.",
      "inputs": [
        {"name": "Unlabeled pretraining corpus", "description": "Large body of domain or general text used to train (or continue-train) the base model with next-token prediction."},
        {"name": "Base model checkpoint", "description": "Either a fresh architecture to train from scratch, or an existing open-weight base ready for continued pretraining."},
        {"name": "Task-specific labeled data", "description": "Smaller, curated dataset for the downstream task — classification labels, instruction/response pairs, or domain Q&A."}
      ],
      "outputs": [
        {"name": "Pretrained base model", "description": "General-purpose checkpoint capable of next-token prediction; reusable across downstream tasks."},
        {"name": "Fine-tuned variant(s)", "description": "One or more task-specialized models — classifier head, instruction follower, domain chatbot — each branching off the same base."}
      ],
      "steps": [
        {
          "name": "Assemble and clean the pretraining corpus",
          "description": "Gather unlabeled text representative of the target domain and general language. Deduplicate, filter low-quality and toxic content, and tokenize with a vocabulary appropriate to the architecture."
        },
        {
          "name": "Pretrain the base with next-token prediction",
          "description": "Train the model on the corpus using a causal language-modeling objective. Track validation perplexity, checkpoint regularly, and stop when held-out loss plateaus."
        },
        {
          "name": "Branch to specialization tasks",
          "description": "Freeze (or version) the base checkpoint. For each downstream task, swap in a task-appropriate head — classification head for label prediction, or keep the LM head for instruction following — and continue training on the task-specific data."
        },
        {
          "name": "Validate each variant against a task-specific eval",
          "description": "Each fine-tuned variant gets its own eval set with task-appropriate metrics (accuracy, F1, instruction-following score). The base model's perplexity does not generalize as a downstream quality signal."
        },
        {
          "name": "Re-use the base for the next task",
          "description": "When a new downstream task arrives, return to the frozen base rather than retraining from scratch. The reusability of the base is the entire economic argument for the methodology."
        }
      ],
      "principles": [
        "Separate knowledge acquisition (pretraining) from behaviour shaping (fine-tuning) so each can be optimized independently.",
        "The base is a reusable asset, not a throwaway intermediate — version and preserve it.",
        "Pretraining loss is not a downstream metric — every fine-tuned variant needs its own task eval.",
        "Specialization is cheap only when the base is good — invest in the base, iterate fast on the heads."
      ],
      "applies_to": ["llm", "llm-app"],
      "related_patterns": ["augmented-llm"],
      "related_methodologies": ["instruction-finetune-then-judge-cycle", "sft-then-dpo-fine-tuning-workflow"],
      "known_failure_modes": [
        {"pattern": "reward-hacking", "note": "Fine-tuning so aggressively on a narrow task eval that the model loses general capability and games the metric."},
        {"pattern": "demo-to-production-cliff", "note": "Shipping a fine-tune that beats the base on the eval set but degrades on real production traffic distributions."}
      ],
      "maturity": "proven",
      "sources": [
        {
          "type": "book",
          "title": "Build a Large Language Model (From Scratch)",
          "url": "https://www.manning.com/books/build-a-large-language-model-from-scratch",
          "chapter": "Ch 5 → Ch 6 / Ch 7",
          "quote": "code a base model, evolve it into a text classifier, and ultimately create a chatbot that can follow your conversational instructions"
        }
      ],
      "verification_status": "verified",
      "date_added": "2026-05-24",
      "last_updated": "2026-05-24",
      "tags": ["pretraining", "fine-tuning", "transfer-learning", "base-model"],
      "diagram": {
        "type": "flow",
        "mermaid": "flowchart TD\n  corpus[Unlabeled corpus] --> clean[Clean + dedupe + tokenize]\n  clean --> pre[Pretrain: next-token prediction]\n  pre --> base[Pretrained base checkpoint]\n  base --> v1[Variant: classifier head]\n  base --> v2[Variant: instruction follower]\n  base --> v3[Variant: domain assistant]\n  data1[Labeled classification data] --> v1\n  data2[Instruction/response pairs] --> v2\n  data3[Domain Q&A] --> v3\n  v1 --> eval1[Classification eval]\n  v2 --> eval2[Instruction-following eval]\n  v3 --> eval3[Domain eval]\n  base -->|new task arrives| reuse[Branch again, do not retrain]"
      },
      "example_scenario": "A team at a mid-sized enterprise has decided they need on-prem language models for three internal tasks: classifying inbound support emails into 14 categories, an instruction-following assistant for internal Q&A, and a domain-specific summariser for legal contracts. They follow the pretrain-then-adapt approach from Raschka's 'Build a Large Language Model (From Scratch)': rather than training three models from scratch or paying for three separate API fine-tunes, they build (or take an open-weight) base once, then specialise it three times.\n\nStage one: they assemble a corpus blending public web text with the firm's internal documents, dedupe and clean it, and pretrain a small base with a causal language-modelling objective; validation perplexity plateaus and the base is checkpointed. Stage two branches three times. For the classifier they swap in a classification head and train on 8,000 labelled emails. For the instruction follower they keep the LM head and fine-tune on 935 (instruction, response) pairs. For the contract summariser they fine-tune on a small curated set of contract/summary pairs. Each variant gets its own task-specific eval; the pretraining perplexity is no longer used as a quality signal. When a fourth task arrives later (entity extraction from invoices), the team returns to the frozen base rather than retraining — the reusability is exactly the economic case for the methodology."
    },
    {
      "id": "instruction-finetune-then-judge-cycle",
      "name": "Instruction Fine-tune Then Judge Cycle",
      "aliases": ["SFT-with-LLM-judge loop", "instruction-tune evaluate iterate"],
      "category": "fine-tuning",
      "summary": "A closed loop for instruction fine-tuning: curate a split instruction dataset (train/validation/test), run SFT while logging per-epoch loss on both train and val, generate responses on the test set, then have a local LLM-as-judge score those responses against the references. The cycle exposes overfitting (val loss diverging from train loss) and quality regressions (judge score dropping) on every iteration, so improvements ship and regressions revert. It is the smallest end-to-end fine-tuning workflow that closes its own quality loop without human grading at every step.",
      "intent": "Iterate on instruction fine-tunes with a model-graded test-set score as the single objective signal, separating training fit from response quality.",
      "when_to_apply": "When you are instruction-tuning a small or mid-size open-weight model and need fast iteration without paying for human grading at every cycle. The dataset is small enough (hundreds to low thousands of examples) that overfitting is a real risk and a held-out test set is essential. Don't apply when the judge model is weaker than the system-under-test (positive-bias collapse), or when the task is so safety-critical that LLM-judge scores cannot be the only quality gate — keep humans in the loop. Exceptions: production deploys still require a human-graded acceptance set before shipping.",
      "inputs": [
        {"name": "Instruction dataset", "description": "Pairs of (instruction, response) split into train/validation/test partitions."},
        {"name": "Base or instruction-tuned checkpoint", "description": "The model to fine-tune — typically an open-weight base or an existing instruct variant."},
        {"name": "Judge model", "description": "A locally runnable model at least as capable as the system-under-test on the target task."}
      ],
      "outputs": [
        {"name": "Fine-tuned model checkpoints", "description": "Per-epoch checkpoints saved during SFT, each tagged with train/val loss."},
        {"name": "Test-set responses", "description": "Generated responses on the held-out test set for the selected checkpoint."},
        {"name": "Aggregate judge score", "description": "Mean and per-example scores from the LLM-as-judge over the test set, used as the primary iteration metric."}
      ],
      "steps": [
        {
          "name": "Curate and split the instruction dataset",
          "description": "Collect (instruction, response) pairs. Reserve a fixed train/validation/test split — e.g. 935 / 55 / 110 — and version the test set so it never drifts across iterations."
        },
        {
          "name": "Run SFT with per-epoch loss logging",
          "description": "Fine-tune with a standard supervised loss. Log train and validation loss at frequent steps so divergence is visible early. Checkpoint per epoch."
        },
        {
          "name": "Generate responses on the test set",
          "description": "With the selected checkpoint, generate responses for every test-set instruction. Persist the (instruction, reference, response) triples for downstream scoring."
        },
        {
          "name": "Score with a local LLM-as-judge",
          "description": "Run the judge model over every triple with a frozen rubric prompt. Emit per-example scores plus an aggregate (e.g. mean over 100 = 51.75)."
        },
        {
          "name": "Diagnose and iterate",
          "description": "Use the loss curves to detect overfitting and the judge score to detect quality issues. Change one variable at a time — dataset, hyperparameters, base model — and re-run the cycle."
        }
      ],
      "principles": [
        "Train/val/test must be fixed before the first run — moving the test set invalidates score comparisons.",
        "Loss curves diagnose fitting; judge scores diagnose quality — both are needed.",
        "The judge model and judge prompt are frozen artefacts; changing either re-baselines history.",
        "Iterate on one variable per cycle so the score change has a unique attribution."
      ],
      "applies_to": ["llm", "llm-app"],
      "related_patterns": ["llm-as-judge", "frozen-rubric-reflection", "eval-harness"],
      "related_methodologies": ["ai-as-judge-evaluation", "evaluation-driven-development"],
      "known_failure_modes": [
        {"pattern": "same-model-self-critique", "note": "Using the fine-tuned model itself as the judge produces inflated scores ceilinged at the model's own blind spots."},
        {"pattern": "reward-hacking", "note": "Iterating against a stale or narrow judge prompt until responses look good to the judge but generalize poorly."}
      ],
      "maturity": "proven",
      "sources": [
        {
          "type": "book",
          "title": "Build a Large Language Model (From Scratch)",
          "url": "https://www.manning.com/books/build-a-large-language-model-from-scratch",
          "chapter": "Ch 7 'Fine-tuning to follow instructions'",
          "quote": "Training set length: 935, Validation set length: 55, Test set length: 110 ... Ep 1 (Step 000000): Train loss 2.611, Val loss 2.668 ... Average score: 51.75"
        },
        {
          "type": "repo",
          "url": "https://github.com/rasbt/LLMs-from-scratch/blob/main/ch07/01_main-chapter-code/README.md"
        }
      ],
      "verification_status": "verified",
      "date_added": "2026-05-24",
      "last_updated": "2026-05-24",
      "tags": ["sft", "instruction-tuning", "llm-as-judge", "iteration-loop"],
      "diagram": {
        "type": "flow",
        "mermaid": "flowchart TD\n  ds[Instruction dataset] --> split[Split train/val/test\\nversion test set]\n  split --> sft[SFT with per-epoch loss logging]\n  sft --> ck[Per-epoch checkpoints\\n+ train/val loss curves]\n  ck --> pick[Pick best checkpoint by val]\n  pick --> gen[Generate responses on test set]\n  gen --> trip[(instruction, reference, response)]\n  trip --> judge[Local LLM-as-judge\\nfrozen rubric prompt]\n  judge --> score[Aggregate + per-example scores]\n  ck --> diag{Loss curves diverge?}\n  diag -->|yes| overfit[Diagnose overfitting]\n  diag -->|no| score\n  score --> iter[Change ONE variable]\n  iter -->|new run| sft"
      },
      "example_scenario": "An engineer is instruction-tuning an open-weight 7B base model on a custom dataset of 1,100 (instruction, response) pairs collected from internal documentation Q&A. They follow Raschka's Chapter 7 closed loop: a fixed split of 935 train / 55 validation / 110 test, with the test set committed to git and never touched between runs. The SFT job logs train and validation loss at every step; in run one the curves diverge cleanly after epoch 2, exposing overfit. The engineer picks the epoch-2 checkpoint, generates responses for all 110 test instructions, and scores each (instruction, reference, response) triple with a locally-hosted Llama 3 70B judge model using a frozen rubric prompt — average score 51.75 over 100.\n\nThe engineer changes exactly one variable per cycle: in run two the learning rate is halved (judge score 56.2); in run three the dataset gets a deduplication pass (judge score 58.9); in run four the prompt template is restructured (judge score drops to 54.1, reverted). Because the train/val/test split, the judge model, and the rubric prompt are all frozen, every score is comparable to every previous score. The engineer ships the run-three checkpoint for an internal beta, with the explicit caveat that the LLM-judge score is a fast iteration signal and not a release gate — a human-graded acceptance set is still run before production exposure."
    },
    {
      "id": "human-feedback-alignment-via-dpo",
      "name": "Human-Feedback Alignment With DPO",
      "aliases": ["direct preference optimization", "DPO alignment"],
      "category": "fine-tuning",
      "summary": "Align an instruction-tuned model to human preferences using Direct Preference Optimization on pairs of (chosen, rejected) responses. DPO replaces the full RLHF stack — reward model training plus PPO — with a single supervised-style objective derived directly from preference pairs. Cheaper to run, easier to debug, and works with the same training infrastructure as SFT. Used after SFT to shape style, helpfulness, and refusal behaviour rather than to teach new tasks.",
      "intent": "Shape model behaviour to match human preferences using a single supervised objective on chosen/rejected pairs, without the operational complexity of full RLHF.",
      "when_to_apply": "After SFT, when the model can perform the task but produces outputs that are stylistically off, overly verbose, unsafe, or otherwise misaligned with preferred behaviour. A preference dataset (chosen, rejected) is available or can be assembled from human raters or stronger-model comparisons. Don't apply when the model fundamentally cannot do the task yet — DPO shapes preferences, it does not teach capabilities; return to SFT first. Exceptions: when reward modelling is genuinely needed (e.g. exploratory RL research), DPO is not a drop-in replacement.",
      "inputs": [
        {"name": "SFT checkpoint", "description": "Instruction-tuned model that already performs the task — DPO refines its preferences, not its capability."},
        {"name": "Preference dataset", "description": "Triples of (prompt, chosen_response, rejected_response) where chosen is preferred over rejected on the target dimension."},
        {"name": "Reference model", "description": "Frozen copy of the SFT checkpoint used as the KL-anchor inside the DPO objective."}
      ],
      "outputs": [
        {"name": "Aligned model checkpoint", "description": "DPO-trained model whose outputs are shifted toward the 'chosen' distribution while staying close to the reference."},
        {"name": "Preference-win-rate evaluation", "description": "Score on a held-out preference set quantifying how often the aligned model is preferred over the SFT baseline."}
      ],
      "steps": [
        {
          "name": "Start from an SFT'd model",
          "description": "Confirm the model can already perform the task. DPO shapes style and preference, not capability — applying it to an unfinetuned base wastes signal."
        },
        {
          "name": "Assemble preference pairs",
          "description": "For each prompt, obtain a (chosen, rejected) pair from human raters, stronger-model comparisons, or curated edits. The signal is the relative ranking, not absolute quality."
        },
        {
          "name": "Freeze a reference model",
          "description": "Snapshot the SFT checkpoint as the reference. DPO's objective is anchored to this reference via a KL term so the model doesn't drift arbitrarily."
        },
        {
          "name": "Train with the DPO objective",
          "description": "Run the DPO loss on the preference pairs against the reference. Single supervised-style training loop — same infrastructure as SFT, no separate reward model, no PPO."
        },
        {
          "name": "Evaluate on a held-out preference set",
          "description": "On unseen prompts, generate from both the aligned model and the reference; have a judge (human or strong LLM) pick the preferred response. Report win-rate as the headline metric."
        }
      ],
      "principles": [
        "DPO shapes preferences over an existing capability — it is not a substitute for SFT.",
        "Preference signal is relative — the ranking matters, not the absolute quality of either response.",
        "The reference model is part of the objective — without a KL anchor the model drifts off-distribution.",
        "Evaluate with held-out preferences, not training loss — DPO loss going down does not imply users prefer the outputs."
      ],
      "applies_to": ["llm"],
      "related_patterns": ["augmented-llm"],
      "related_methodologies": ["sft-then-dpo-fine-tuning-workflow", "instruction-finetune-then-judge-cycle"],
      "known_failure_modes": [
        {"pattern": "reward-hacking", "note": "Optimising preference pairs too aggressively produces responses that game the rater's superficial cues without genuine improvement."},
        {"pattern": "sycophancy", "note": "Preference pairs labelled by raters who reward agreement push the model toward agreeable but inaccurate responses."}
      ],
      "maturity": "emerging",
      "sources": [
        {
          "type": "book",
          "title": "Build a Large Language Model (From Scratch)",
          "url": "https://www.manning.com/books/build-a-large-language-model-from-scratch",
          "chapter": "Ch 7 bonus / Appendix (DPO implementation)",
          "quote": "Use human feedback to ensure your LLM follows instructions ... Bonus: Direct Preference Optimization (DPO) implementation"
        }
      ],
      "verification_status": "verified",
      "date_added": "2026-05-24",
      "last_updated": "2026-05-24",
      "tags": ["dpo", "preference-alignment", "rlhf-alternative", "post-sft"],
      "diagram": {
        "type": "flow",
        "mermaid": "flowchart LR\n  sft[SFT checkpoint] --> ref[Freeze copy as reference]\n  sft --> tr[DPO trainer]\n  pairs[Preference pairs:\\n(prompt, chosen, rejected)] --> tr\n  ref -->|KL anchor| tr\n  tr --> aligned[DPO-aligned checkpoint]\n  aligned --> eval[Held-out preference set]\n  ref --> eval\n  eval --> judge[Human or strong-LLM judge]\n  judge --> wr[Win-rate vs reference]\n  wr -->|win-rate > baseline| ship[Ship aligned]\n  wr -->|no improvement| diag[Investigate preference data]\n  diag --> pairs"
      },
      "example_scenario": "A team has already produced an SFT checkpoint for a domain assistant that performs the underlying Q&A task competently but is too verbose, occasionally over-apologetic, and inconsistent in tone. Rather than running full RLHF with a separately-trained reward model and PPO, they apply DPO. Annotators are given 4,000 prompts and shown two responses each (one from the SFT model, one rewritten by a stronger model or a human editor); they pick the preferred response, producing 4,000 (prompt, chosen, rejected) triples. The SFT checkpoint is snapshotted as the frozen reference for the KL anchor.\n\nThey run DPO using the same training infrastructure as SFT — no separate reward model, no PPO loop — and produce an aligned checkpoint. Evaluation is by win-rate on a held-out preference set of 500 prompts: a strong external LLM judge compares aligned versus reference responses pairwise and reports 64% aligned-wins, 22% reference-wins, 14% ties. The team confirms with a smaller human-graded sample that the LLM-judge ranking tracks the human ranking before shipping. Crucially, they did not apply DPO to the base model directly — early experiments showed that DPO cannot teach a capability the SFT stage did not already install; trying to skip SFT collapsed the model. DPO shapes how the existing capability is expressed, not what the model knows how to do."
    },
    {
      "id": "sft-then-dpo-fine-tuning-workflow",
      "name": "SFT Then DPO Fine-tuning Workflow",
      "aliases": ["two-stage alignment", "instruct-then-align"],
      "category": "fine-tuning",
      "summary": "A two-stage open-weight fine-tuning workflow: first run supervised fine-tuning on an instruct dataset to teach task structure and instruction-following, then run Direct Preference Optimization on preference pairs to refine style, helpfulness, and safety. A single YAML configuration switches between the stages so the same training infrastructure serves both. Sequencing matters — SFT installs the capability, DPO shapes how that capability is expressed; skipping or reordering them produces a model that is either incapable or off-style.",
      "intent": "Move an open-weight base to a production-ready aligned assistant in two well-defined stages, each with its own dataset and objective, sharing one training pipeline.",
      "when_to_apply": "Building or customizing an open-weight assistant for a domain where both task capability and a specific tone, safety profile, or interaction style matter. You control the training stack and have, or can build, both an instruct dataset and a preference dataset. Don't apply when an API instruct model already meets your needs — the pipeline complexity is not justified. Exceptions: regulated deployments forcing on-prem weights, or research investigating alignment trade-offs across the SFT/DPO boundary.",
      "inputs": [
        {"name": "Open-weight base model", "description": "Pretrained base checkpoint suitable for instruction fine-tuning."},
        {"name": "Instruction dataset", "description": "Curated (instruction, response) pairs used by the SFT stage to install task structure."},
        {"name": "Preference dataset", "description": "Triples of (prompt, chosen, rejected) used by the DPO stage to align style and safety."},
        {"name": "Pipeline configuration", "description": "YAML config selecting the stage (SFT vs DPO), hyperparameters, dataset paths, and output directory."}
      ],
      "outputs": [
        {"name": "SFT checkpoint", "description": "Instruction-tuned model from stage one; reusable as the entry point for DPO and as a baseline for evaluation."},
        {"name": "DPO-aligned checkpoint", "description": "Final aligned model from stage two; ready for production evaluation."},
        {"name": "Stage-comparison eval report", "description": "Side-by-side scores on a held-out eval comparing base, SFT, and DPO checkpoints to attribute gains to each stage."}
      ],
      "steps": [
        {
          "name": "Specify the two stages in one config",
          "description": "Author a single YAML that defines both stages with their datasets, hyperparameters, and outputs. The same training entry point reads the config and dispatches to SFT or DPO."
        },
        {
          "name": "Run SFT on the instruct dataset",
          "description": "Stage one: fine-tune the base on (instruction, response) pairs to install task structure and instruction-following. Track loss, save per-epoch checkpoints, pick the best by validation."
        },
        {
          "name": "Evaluate the SFT checkpoint independently",
          "description": "Before moving on, confirm SFT actually taught the task. If the SFT model can't perform the task, DPO will not rescue it — return to dataset or hyperparameter changes."
        },
        {
          "name": "Run DPO from the SFT checkpoint",
          "description": "Stage two: with the SFT checkpoint as both starting point and reference, train on preference pairs to refine style and safety. Keep the KL anchor strong enough that capability isn't lost."
        },
        {
          "name": "Compare base, SFT, and DPO on a held-out eval",
          "description": "Run all three checkpoints on the same eval set. Attribute capability gains to SFT and style/preference gains to DPO. If DPO is not adding measurable preference win-rate, investigate the preference data."
        }
      ],
      "principles": [
        "Sequence matters: SFT teaches capability, DPO shapes preference — never reverse them.",
        "Each stage owns its own dataset and its own metric — capability eval for SFT, preference win-rate for DPO.",
        "A shared YAML config is the contract — it makes the workflow reproducible and the stages comparable.",
        "Evaluate every stage independently before composing — a bad SFT checkpoint guarantees a bad DPO outcome."
      ],
      "applies_to": ["llm", "llm-app"],
      "related_patterns": ["augmented-llm"],
      "related_methodologies": ["human-feedback-alignment-via-dpo", "pretrain-then-adapt-methodology", "instruction-finetune-then-judge-cycle"],
      "known_failure_modes": [
        {"pattern": "demo-to-production-cliff", "note": "Aligned model wins on the preference eval but degrades on production traffic distributions that the preference set under-represents."},
        {"pattern": "reward-hacking", "note": "DPO with weak preference labels collapses the model toward responses that exploit superficial labeller cues."}
      ],
      "maturity": "emerging",
      "sources": [
        {
          "type": "book",
          "title": "LLM Engineer's Handbook",
          "url": "https://www.packtpub.com/en-us/product/llm-engineers-handbook-9781836200079",
          "chapter": "Ch 5 'Supervised Fine-tuning'; Ch 6 'Fine-tuning with Preference Alignment'",
          "quote": "Chapter 5: Supervised Fine-tuning ... Chapter 6: Fine-tuning with Preference Alignment"
        },
        {
          "type": "repo",
          "title": "PacktPublishing/LLM-Engineers-Handbook",
          "url": "https://github.com/PacktPublishing/LLM-Engineers-Handbook",
          "quote": "The system supports both supervised fine-tuning and DPO (direct preference optimization) workflows configurable through YAML files"
        }
      ],
      "verification_status": "verified",
      "date_added": "2026-05-24",
      "last_updated": "2026-05-24",
      "tags": ["sft", "dpo", "two-stage", "alignment-workflow"],
      "diagram": {
        "type": "flow",
        "mermaid": "flowchart LR\n  yaml[YAML config:\\nstage, dataset, hparams] --> entry[Training entry point]\n  base[Open-weight base] --> stage1\n  inst[Instruction dataset] --> stage1[Stage 1: SFT]\n  entry --> stage1\n  stage1 --> sft[SFT checkpoint]\n  sft --> eval1{Capability eval pass?}\n  eval1 -->|no| fixdata[Fix instruct data / hparams]\n  fixdata --> stage1\n  eval1 -->|yes| stage2[Stage 2: DPO]\n  pref[Preference dataset] --> stage2\n  sft -->|reference + start| stage2\n  entry --> stage2\n  stage2 --> aligned[DPO-aligned checkpoint]\n  aligned --> compare[Compare base vs SFT vs DPO]\n  sft --> compare\n  base --> compare\n  compare --> ship[Production candidate]"
      },
      "example_scenario": "A team building a customised open-weight assistant for a regulated industry follows the two-stage SFT then DPO workflow from the LLM Engineer's Handbook. They write a single YAML configuration with two sections, one per stage, each pointing at its own dataset, hyperparameters, and output directory. The same Python entry point reads the YAML, dispatches to the SFT or DPO trainer, and writes checkpoints into a stage-tagged directory.\n\nStage one fine-tunes the base on an instruct dataset of 12,000 (instruction, response) pairs sourced from internal documentation; the SFT checkpoint is evaluated independently on a capability eval (task accuracy, instruction-following score) and only released to stage two when it clearly outperforms the base. Stage two runs DPO from the SFT checkpoint, using it both as the starting point and as the KL-anchored reference; the preference dataset of 6,000 (prompt, chosen, rejected) triples is annotated by a small team of domain experts plus rewrites from a stronger model. The team compares base, SFT, and DPO on the same held-out eval — SFT delivers most of the capability gain, DPO adds preference win-rate and tone consistency. When an early experiment skipped SFT and applied DPO directly to the base, the resulting model could not perform the task and DPO 'aligned' it to choose between equally-bad responses; this confirmed the sequencing rule and is documented in the team's runbook as a never-do-again experiment."
    }
  ]
}
